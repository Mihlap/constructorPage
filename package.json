{
  "name": "page-builder",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm -w backend run dev\" \"npm -w frontend run dev\"",
    "build": "npm -w backend run build && npm -w frontend run build",
    "lint": "npm -w backend run lint && npm -w frontend run lint",
    "test": "npm -w backend run test && npm -w frontend run test",
    "test:watch": "npm -w backend run test:watch"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
